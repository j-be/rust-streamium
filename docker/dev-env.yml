version: '3.1'

services:

  db:
    image: postgres
    restart: always
    ports:
      - 5432:5432
    environment:
      POSTGRES_DB: rust-streamium-dev
    networks:
      - rust-streamium

networks:
  rust-streamium: